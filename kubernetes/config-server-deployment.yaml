apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-server
  namespace: devops

spec:
  replicas: 1

  selector:
    matchLabels:
      app: config-server

  template:
    metadata:
      labels:
        app: config-server

    spec:
      containers:
      - name: config-server
        image: devops/config-server:1.0
        imagePullPolicy: IfNotPresent

        ports:
        - containerPort: 8888
