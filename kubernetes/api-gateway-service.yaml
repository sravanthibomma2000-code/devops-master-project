apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: devops

spec:
  selector:
    app: api-gateway

  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082

  type: ClusterIP