apiVersion: v1
kind: Service
metadata:
  name: {{ include "iot-platform-chart.fullname" . }}
  labels:
    {{- include "iot-platform-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iot-platform-chart.selectorLabels" . | nindent 4 }}
